# GitHub OAuth App Configuration (for Web OAuth Flow)
#
# Production Deployment (Vercel):
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Fill in:
#    - Application name: AI Adoption Leaderboard
#    - Homepage URL: https://your-app.vercel.app
#    - Authorization callback URL: https://your-app.vercel.app/api/auth/github/callback
# 4. Click "Register application"
# 5. Copy the Client ID below
# 6. Generate a Client Secret and add it to Vercel Environment Variables
#
# The NEXT_PUBLIC_ prefix makes the Client ID available in the browser (safe).
# The Client Secret is ONLY used server-side and should NEVER be exposed.

NEXT_PUBLIC_GITHUB_CLIENT_ID=your_oauth_app_client_id

# IMPORTANT: Set this in Vercel Environment Variables (not in .env files!)
# GITHUB_CLIENT_SECRET=your_oauth_app_client_secret
